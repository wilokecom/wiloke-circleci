version: 2.1
references:
  - workspace_root: &workspace_root
                      /tmp
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01
    working_directory: ./var/www/html
    steps:
      - checkout
workflows:
  build_test_deploy:
    jobs:
      - build
